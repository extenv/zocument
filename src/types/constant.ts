// Define constants used across the application

// Configuration file name
export const CONFIG_FILE_NAME = 'zocument.config.js';

// Default directories
export const DEFAULT_DOCS_DIR = 'docs';
export const ARCHIVE_DIR = 'archive';
export const ASSETS_DIR = 'assets';
export const CONTENTS_DIR = 'contents';

// Default output directory for built files
export const DEFAULT_OUTPUT_DIR = 'public';

// Supported themes
export const SUPPORTED_THEMES = ['classic', 'modern'] as const;
export const DEFAULT_THEME = 'classic';

// Default version if none is specified
export const DEFAULT_VERSION = '1.0.0';
